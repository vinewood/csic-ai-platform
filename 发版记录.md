# 发版记录（CHANGELOG）— 中船党校 AI 智能业务平台

> 维护人：Kimi（2026-07-25 接手）| 格式：版本 / 日期 / 变更 / 验证

## v3.3.3 · 2026-07-31 · 高分辨率屏幕适配：全宽布局 + 视口高度修正

### 问题定位（MacBook 高分辨率屏界面不铺满）
- 顶栏 `.bar-inner` 与主内容 `.content-inner` 写死 `max-width: 1440px`，宽屏（逻辑分辨率 >1440）两侧出现空白带
- 每日资讯 `.news-layout` 更窄（1200px）且瀑布流固定 3 列，宽屏空间浪费
- Chat/Teaching `.chat-layout` 高度按 `100vh - 160px`、Research 按 `100vh - 200px` 估算，实际占位仅约 98px/142px，底部留白 60–100px

### 修复
- DefaultLayout：顶栏与主内容去除 1440px 上限，全宽自适应（保留 24px 内边距）
- News：布局去 1200px 上限；瀑布流列数随屏宽递增 —— ≥1400px 四列、≥1900px 五列（原固定 3 列）
- Skills / Knowledge：卡片网格新增 `lg`/`xl` 断点 —— ≥1200px 每行 4 张、≥1920px 每行 6 张（原最多 4 张/3 张）
- Chat/Teaching 高度改为 `100vh - 102px`、Research 改为 `100vh - 142px`（按顶栏 58 + 内容 padding 40 + 页内 topbar 41 实算），垂直方向贴满视口

### 验证
- `npm run build` 通过（2.06s）；部署后 https://csic.thinkalike.com.cn/ 返回 200
- 纯 CSS/布局改动，后端未动

## v3.3.2 · 2026-07-28 · 全面盘点：4 个前端 bug 修复 + 服务器清理

### 修复（前端）
- **每日资讯日期筛选差一天**：`el-date-picker` 选中的本地日期用 `toISOString()`（UTC）格式化，UTC+8 下请求参数比所选日期少一天；改为本地时区手动格式化（News.vue）
- **每日资讯摘要渲染双重解析**：抽屉内 `v-html="cleanHtml(...)"` 先解码 HTML 实体再被 v-html 二次解析（`&lt;` 变回 `<` 被当标签、换行丢失且有注入面）；改为纯文本插值 + `white-space: pre-wrap`，抽屉标题同步走 cleanHtml（News.vue）
- **技能中心跳转技能未挂载**：「教学使用」「科研使用」按钮跳转携带 `?skill=`，但 Teaching.vue / Research.vue 从未消费该参数；现两页 onMounted 读取并自动挂载（用列表原始 id 保证类型一致），Chat.vue 的 el-select 同步修复严格匹配显示技能名称而非裸 id
- **个人设置首次打开表单为空**：profileForm 在 onMounted 同步拷贝时 userInfo 尚未异步加载完成；改为打开弹窗时（openProfile）再拷贝（DefaultLayout.vue）

### 清理（服务器）
- 删除历史错误路径残留的空数据库 `backend/data/csic.db`（真实库在项目根 `data/csic.db`，v3.2.2 已统一）
- 删除未使用 Docker 镜像（dotnet/aspnet 320MB、rabbitmq 227MB 及其停止容器），磁盘 67%→66%
- 发现待决策：`brave_black` 容器运行 zaproxy（镜像 3.6GB，状态 unhealthy），非本项目资产，疑似 workbuddy 时期 UniGroup 安全扫描残留，占用内存——待用户确认后移除

### 验证
- `npm run build` 通过（2.03s）；部署后 https://csic.thinkalike.com.cn/ 返回 200
- 纯前端修复，后端未改动无需重启

## v3.3.1 · 2026-07-28 · 在线接口文档（ReDoc）+ docsify 帮助中心 + 顶栏入口

### 新增（文档体系 — 主流开源工具，自动同步）
- **接口文档**：启用 FastAPI 运行时自动生成的 ReDoc（`/redoc`，阅读友好）+ 已有 Swagger UI（`/docs`，可调试）+ `/openapi.json`；三者由代码运行时生成，**代码更新部署即自动同步**，无需手工维护
- **帮助中心**：docsify（本地化运行库，零 CDN 依赖）挂载 `/help/`；markdown 源文件放 `frontend/public/help/`，**每次 `npm run build` 自动随构建产物同步上线**；含 首页/快速上手/功能指南/接口文档/MCP接入指南/常见问题 6 页，带全文搜索、侧栏、图片缩放
- nginx：新增 /redoc 反代；/help/ 单独 location 禁缓存（主站 location / 带 1 年 immutable，Markdown 更新必须绕过）

### 新增（顶栏入口）
- DefaultLayout 右上角用户头像左侧新增「帮助」（/help/）与「接口」（/redoc）图标链接，新窗口打开；移动端自动收缩为纯图标

### 验证
- /help/ 200、首页与中文文件名 Markdown 正常拉取；/redoc 200；首页 200

## v3.3.0 · 2026-07-28 · MCP 接口上线 + 接口文档 + 全面优化第三批

### 新增（MCP 接口 — 供 hibuddy 等 Agent 对接）
- 标准 Model Context Protocol（Streamable HTTP，2025-03-26）端点 `POST /mcp/`，官方 mcp SDK FastMCP 实现
- 5 个工具：`csic_chat`（多模型对话）/ `csic_knowledge_search`（知识库 RAG）/ `csic_academic_search`（OpenAlex）/ `csic_daily_news`（RSS 日报）/ `csic_list_skills`（技能列表）
- 认证：Bearer 平台 JWT 或 MCP 静态令牌（api_configs.mcp，已生成 `mcp_` 前缀令牌入库，铁律未进 git）
- 挂载细节：FastMCP session_manager 并入宿主 lifespan；SDK 默认 DNS 重绑定防护会拒域名 Host，已显式白名单；nginx 新增 /mcp 反代（SSE 关缓冲、300s 读超时）
- 全流程实测：initialize → initialized → tools/list → tools/call ×5 全部真实数据返回；无令牌 401

### 新增（文档）
- `docs/接口文档.md`：Base URL、认证、17 组接口一览、核心接口用法、错误码约定
- `docs/MCP接入指南.md`：端点/令牌、客户端配置示例、握手流程（含 Git Bash 中文 GBK 坑提示）、工具清单、hibuddy 对接要点
- Admin「系统管理」新增「开放接口」标签页：Swagger/OpenAPI/MCP 端点信息与文档链接

### 优化（批 3）
- News 卡片：分类色顶条（--card-accent 注入）、来源改胶囊样式、摘要 CSS line-clamp 4 行省略（替代 JS 截断 120 字）、悬停阴影/位移对齐设计令牌
- Admin：新增用户对话框的"默认 Xsx2024"过时占位文案改为"留空则生成随机初始密码"（v3.2.0 行为）

### 验证
- MCP 五工具 E2E 全通；首页 200；/docs 200

## v3.2.4 · 2026-07-26 · 全面优化第二批：空/加载/错误三态统一

### 优化（三态统一）
- global.css 新增共享三态样式：`.csic-empty`（标准空态：图标+标题+引导文案）/`.csic-empty--sm`（侧栏小空态）/`.csic-loading`（旋转点+文案）/`.csic-error`（错误说明+重试容器）
- 替换各处散装空态：News 主区（原 80px 灰字段落）、Knowledge 数据集空态（补充引导文案）、Chat/Research/Teaching 侧栏"暂无历史"内联样式 ×3、Teaching "暂无技能"
- 清理死样式：News `.empty-state` + 未用的 Document 图标导入、Chat `.conv-empty`
- Chat 主空态（logo+快捷提问）与 Teaching 主空态为页面定制设计，保留不动

### 验证
- 构建通过；首页 200

## v3.2.3 · 2026-07-26 · legacy 直连收尾 + 全面优化第一批（hero 去外链）

### 确认（AMiner 无需重新申请）
- api_configs 中 workbuddy 时期保存的 AMiner JWT key 实测有效（Bearer/裸 key 均 200），/api/academic/aminer/* 全部恢复可用；此前"未配置"只是 research.py 走了另一套需重新签名的实现

### 修复（legacy 端点直连）
- /api/chat/stream 移除"Dify 优先、失败再 fallback"路径（原 Dify 应用已删除，每次请求先撞死服务增加首字延迟），直接走 MODEL_ENDPOINTS 直连；默认模型 qwen→deepseek 与全站统一；清理无用的 DifyService 导入

### 全面优化（第一批：去外部依赖）
- Video/Skills/Admin 三页 hero 横幅的 Unsplash 外链背景（国内加载慢且随时可能失效）替换为纯 CSS 主题渐变 + 网点/斜线纹理：`.csic-hero--video`（深青影像）/`.csic-hero--skills`（靛蓝技能）/`.csic-hero--admin`（石板蓝管理），全站图片零外部依赖

### 验证
- /api/chat/stream 直连流式正常；/api/academic/aminer/scholar 返回真实学者数据；首页 200

## v3.2.2 · 2026-07-26 · 全局排查修复（科研检索复活 + 视频 ASR 端到端 + 数据库路径统一）

### 修复（科研综合检索 /api/research/search — 此前三层只活 AI 一层）
- Arxiv 层：`http://export.arxiv.org` 返回 301 而 httpx 默认不跟随重定向 → 静默失败；改 https + `follow_redirects=True`，恢复真实论文检索
- 学者层：AMiner person/search 已全面要求鉴权（40308），旧调用永远失败；改为 OpenAlex 学者检索（免费免 Key）为主、AMiner 兜底，返回 `scholar_source` 标明来源
- 两层裸 `except: pass` 改为 `errors[]` 透传（X-Search-Data 头可见），失败不再装死

### 修复（视频分析 — ASR 端到端首次真正打通）
- 服务器安装 ffmpeg + venv 安装 dashscope（此前两者皆缺，ASR 必挂）
- ASRService 重写：密钥走统一配置缓存（bailian/dashscope/qwen）；ffmpeg 抽取 16k 单声道 wav（此前把 mp4 直接塞给 ASR）；模型用 paraformer-realtime-v2（实测本账号百炼仅此模型有权限，v1/v2 文件版 Model not found）；适配新版 SDK 必传 callback；get_sentence() list/dict 兼容；临时 wav 用后清理
- video.py：ASR 失败 → 任务 `failed` 如实标记并写明原因（此前把错误文本当转写稿喂给 AI 生成摘要）；摘要"生成中..."假状态改为失败明示
- 摘要/闪卡从 Dify chat-messages 改走 MODEL_ENDPOINTS 直连（原 Dify 应用已被删除，调用必然 403 "The app no longer exists"）；删除静默失败的 upload_file 死代码
- 前端 Video.vue 轮询补 `failed` 状态分支（此前失败任务会一直转到超时）

### 安全（密钥治理）
- 清除 integrations.py 硬编码 Dify key（昨日清洗后服务器上已是 ***REMOVED 占位，该路径实际瘫痪）；真实 app token 从 Dify Postgres 的 api_tokens 表捞出入库 `api_configs(dify_app)`，源码零硬编码
- `_get_dify_key` 修复查错列名（`value` → config_json），改为 env → dify_app → dify 顺序

### 重构（数据库路径统一 — 一地鸡毛清理）
- 全后端 9 处 sqlite3.connect 统一为 `app.config.DATA_DIR`：academic_search.py（2 处绝对路径）、kb_storage.py（4 处）、dify_kb.py（3 处）、aminer_service.py（3 层 parent 推导错位）、integrations.py（cwd 相对路径会读到 backend/data 的空库）
- kb_storage 上传目录同步改用 config.UPLOAD_DIR

### 验证
- research/search：OpenAlex 学者 5 条 + Arxiv 论文 5 条全活，errors 为空
- 视频流水线 E2E：真实英文语音 wav → ASR 转写成功 → DeepSeek 中文摘要 + Q&A 闪卡 JSON 生成，任务 done，测试数据已清理
- 回归：登录 / 对话流式 / Dify 知识库检索 / 视频列表 / 首页全 200

## v3.2.1 · 2026-07-26 · 多模型选择与宫格一体化（交互重构）

### 交互重构（选择即布局）
- 废除"单/多模型 radio + 下拉多选 + 手工 2/4/6 宫格切换"三步操作，改为**模型 chips 点选**：点选 1 个=单模型对话，点选 2-6 个自动进入宫格对比，取消选中回到单聊（至少保留 1 个，超过 6 个提示上限）
- 宫格布局按模型数量自动推导：2→2列、3→3列、4→2×2、5/6→3×2；窄屏（≤900px）强制 2 列；区块最大化/还原能力保留
- 工具栏实时提示当前状态（"已选 N 个模型 · 自动 N 宫格" / "单模型对话 · 再点选模型即自动宫格对比"）
- 应用范围：教学工作台（Teaching.vue）与 AI 对话页（Chat.vue）两处同步改造，交互完全一致

### 代码质量
- 新增共享模块 `frontend/src/utils/models.js`：统一全站模型标签与主题色（MODEL_META / modelLabel / modelColor），消除 Chat/Teaching/ModelCompareGrid 三处重复定义
- ModelCompareGrid 删除 gridMode prop 与 GRID_DEFS，布局纯由 panes.length 推导（AUTO_LAYOUT 表）
- 两个页面的 mode/model/multiModels 由三个独立 ref 收敛为 picked 单一数据源 + computed 派生（读写兼容历史会话回放）

### 验证
- 构建通过；线上 bundle 确认含新代码（model-chip / 自动宫格对比，gridMode 残留 0）
- 回归：登录、/api/chat/dify-chat 流式对话正常（deepseek-v4-flash）

## v3.2.0 · 2026-07-25 · 第二周：真实化攻坚（假数据清零 + 密码安全 + 知识库语料 + RSSHub 上线）

### 安全（密码与凭证）
- Dify 控制台密码改为从服务器保险箱读取（env `DIFY_ADMIN_PASSWORD` / api_configs.dify.console_password），源码零硬编码；`/api/dify/init` 不再回传凭证
- 废除固定默认密码：`UserCreate.password` 默认空 → 后端生成一次性随机初始密码（仅本次响应显示）；`seed.py` 初始密码走 `ADMIN_PASSWORD` 环境变量或随机生成打印一次
- `check_dify_db.py` 硬编码 Postgres 密码 → 环境变量 `DIFY_DB_PASSWORD`
- 铁律更新：密码绝不明文存储/传输（bcrypt 哈希 + HTTPS）；鉴权审计覆盖每一个 router（memory 第〇章 + SKILL 第〇条）

### 修复（假数据清零 — 失败即明示）
- 删除教学课题/灵感的模板假数据回退：AI 失败或返回非 JSON → 502 如实报错（此前 HTTPException 被 except 吞掉，503 缺 key 也会冒出模板课题）
- 删除学术服务假评分（评估失败曾返回硬编码 82/78 分套话）与 `_fallback_topics` 模板选题
- 修复**科研工作台的隐藏炸弹**：`_ai_stream`/`research_chat` 无视 model 参数且永远发 `deepseek-chat`（该账号不存在此模型）→ 全部科研 AI 功能必然报错；现统一走 MODEL_ENDPOINTS 路由；academic_service、rss 日报同款模型名一并修正为 deepseek-v4-pro

### 重构（对话历史 DB 单轨）
- 删除 dify_service 内存字典（chat_histories/conversation_store）：历史从 messages 表按会话读取，assistant 按模型过滤（多模型轮次互不串台），末尾与当前 query 重复的用户消息自动剔除
- 删除对话改为真实删除 DB 会话+消息（旧实现只清内存字典，DB 残留）
- 已验证：第二轮对话准确回忆起第一轮暗号（DB 上下文恢复 ✅）

### 新增（知识库语料 · 真实数据）
- 全网权威来源抓取 7 篇典型党校语料灌入知识库（scripts/seed_dify_kb.py，可复跑可过滤）：
  - 党建知识库：党章（二十大修订）、纪律处分条例（2023 修订）、党内政治生活若干准则
  - CSIC政策文件：二十大报告全文、二十届三中全会《决定》
  - 教学资源库：党校（行政学院）工作条例（2025 修订）
  - 船舶工程资料：船舶制造业绿色发展行动纲要（2024—2030）

### 修复（资讯链路）
- RSS 抓取串行无超时 → 并发 + 单源 10s 超时（17 个源 6 个是 HTML 页，旧实现必 504）
- nginx `/api/` proxy_read_timeout 提至 150s；RSSHub 以 tsx 直启上线（systemd rsshub.service，MemoryMax 800M，开机自启）——4GB 小机放弃本地构建（tsdown 构建超 5 分钟被打断）

### 线上验证
- 教学课题生成真实 AI 结果（党纪学习教育 → 《条例》专题辅导等）✅
- 科研台流式真实输出 ✅；对话上下文 DB 恢复 ✅；每日资讯 46s 生成真实简报 ✅
- Dify 知识库列表正常（console_password 修复后）✅；后台集成状态 RSSHub=online ✅

---

## v3.1.2 · 2026-07-25 · 全站审计第一轮：前端假功能修复

### 修复（Bugfix）
- 每日资讯「日期筛选」失效：`News.vue` 调用 `apiGet(path, { params })`，但 api.js 的 apiGet 只接收一个参数，date 从未发出 → 改为手动拼 query string
- 视频解析「导出文本 / 分享」是假功能：前端调用的 `/api/video/export`、`/api/video/share` 后端路由不存在，且 `.then/.catch` 都提示成功 → 改为纯前端真实实现（导出 Markdown 下载 / 剪贴板复制链接，失败有真实提示）

### 安全复核（无代码变更，确认 v3.1.1 生效）
- 无 token 访问 /api/models 等受保护接口 → 401 ✅；key 不再明文回传 ✅

### 线上验证
- 登录 ✅ / 资讯接口带 date 参数 200 ✅ / 视频列表 200 ✅ / 对话流式（deepseek-v4-flash）✅
- 新前端产物已部署；旧 assets 已改名备份可回滚

### 审计遗留（已排期，见摸底报告）
- P1：academic_service/teaching 的 AI 失败静默回退模板假数据、假评分（82/78 硬编码）
- P1：dify_service 内存字典与 DB 双轨存历史，需统一
- P2：构建 chunk >500kB 警告、服务器旧 assets 备份待清理

---

## v3.1.0 · 2026-07-25 · 安全止血 + 多模型宫格大改版

### 安全（P0）
- 移除源码中全部硬编码 API Key（dify_service.py / setup_bailian.py / Admin.vue），运行时统一从服务器 `api_configs` 表读取
- CONFIG.md 移出 git 跟踪，.gitignore 封锁一切 key/密码文件（铁律：key 只存本地与服务器）
- git filter-repo 清洗全部 62 个历史 commit + GitHub 删库重建，旧泄露 key 已轮换失效

### 修复（Bugfix）
- 修复 qwen 模型流式无输出：删除错误的 DashScope 原生格式解析分支，百炼 compatible-mode 统一按 OpenAI 格式解析
- 修复 deepseek 400 错误：`deepseek-chat` → `deepseek-v4-pro`（该账号仅支持 v4 系列）
- 修复 teaching.py 模型路由：`_ai_call` 不再无视 model 参数，统一走 MODEL_ENDPOINTS 路由
- 实测确认百炼模型名全部真实可用：qwen3.7-plus / qwen3.7-max / qwen3.6-flash / glm-5.2 / kimi-k2.7-code / MiniMax-M2.5

### 新增（Feature）
- AI 对话 · 多模型对比全新交互：2 宫格 / 4 宫格 / 6 宫格布局切换；每个模型区块可最大化（全屏聚焦）与还原；区块头部含模型标识与操作按钮
- 多模型宫格抽取为共享组件 `frontend/src/components/ModelCompareGrid.vue`，AI 对话页与教学工作台复用同一实现

### 修复（Bugfix·第二轮）
- 教学工作台多模型对比此前是消息流内嵌窄格子、且无最大化能力 → 已接入宫格工作台
- 修复 Teaching.vue `apiPost` 未导入：「整理成技能」功能必崩
- 修复多模型模式会话 id 未回传：N 个模型并行请求各自新建会话（会话列表被刷爆）→ 共享组件改为首发请求先建会话、拿到 id 再并发其余

### 优化（UI/UX）
- 全站视觉统一打磨：布局、横幅、卡片、空状态、加载态

### 线上验证
- 登录 ✅ / deepseek ✅ / qwen ✅ / glm-4 ✅ / qwen-plus ✅ / qwen-max ✅ / qwen-turbo ✅ / kimi ✅ / minimax ✅
- teaching/generate 经 qwen（百炼）路由返回真实 AI 课题 ✅（旧代码此路径必挂）
- 新前端构建产物（Chat-BtA3ZS4V.js）线上 200 ✅；旧 assets 备份为 assets_bak_20260725 可回滚
- 多模型宫格：2/4/6 宫格切换、区块最大化/还原、并行流式回答（前端交互，浏览器验证）

---

## v3.0.0 · 2026-07-13（workbuddy 时期最后版本）
- Vue3 SPA 定稿；教学对话历史 conversation_id 贯通；20 项冒烟测试通过

## v2.x · 2026-07-04 ~ 07-10（workbuddy 时期）
- FastAPI + Dify + 种子数据；系统设置全真实化；SSL 上线

## v3.1.1 · 2026-07-25 · 多模型历史对比重建

### 修复（Bugfix）
- 点击历史对话在多模型模式下不出宫格/内容：根因是 messages 表未保存 model 字段且多模型广播时每个请求重复保存用户消息
- 后端：用户/AI 消息均落 model 字段；ChatRequest 新增 save_user（仅首发请求保存用户消息）；历史接口返回 model
- 前端：ModelCompareGrid 新增 loadRounds 按轮次重建宫格；Teaching/Chat 点击多模型历史自动切多模型视图并重建，旧无 model 历史回退单视图并提示
- 产品约定：多模型对比默认主页面为「教学工作台」，AI 对话页复用同一共享组件

### 线上验证
- 模拟双模型对比一轮 → 历史结构完美：1 条 user（model=deepseek）+ 2 条 assistant（model=deepseek/qwen 各一）✅
